function Saved() {
  return (
    <section className="page" aria-labelledby="saved-title">
      <h1 id="saved-title">Saved Homes</h1>
      <p>Your bookmarked listings live here so you can revisit them whenever you are ready.</p>
    </section>
  );
}

export default Saved;
